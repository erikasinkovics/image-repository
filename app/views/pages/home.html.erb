<div class="container home-container">
  <div class="hero-container">
    <h1>Welcome to My Image Repository!</h1>
    <h2>Feel free to browse the images uploaded by our creators, or log in to upload your own photos!</h2>
 </div>

  <% if user_signed_in? %>
    <%= link_to "Images#index", user_images_path(current_user) %>
    <br>
    <%= link_to "Images#new", new_user_image_path(current_user) %>
  <% else %>
    <%= link_to "Create an account", new_user_registration_path, class: "btn-white" %>
  <% end %>
</div>
